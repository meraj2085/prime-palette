export enum tagTypes {
  user = "user",
  department = "department",
  service = "service",
  feedback = "feedback",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.department,
  tagTypes.service,
  tagTypes.feedback,
];

export enum tagTypes {
  user = "user",
  service = "service",
  feedback = "feedback",
  appointment = "appointment",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.service,
  tagTypes.feedback,
  tagTypes.appointment,
];
